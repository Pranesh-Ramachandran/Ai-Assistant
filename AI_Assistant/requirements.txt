# ═══════════════════════════════════════════════════════════════
# JARVIS AI Assistant — Unified Requirements
# All free/open-source, no paid API keys required
# ═══════════════════════════════════════════════════════════════

# ── Core UI ──────────────────────────────────────────────────
kivy>=2.1.0
plyer>=2.1.0

# ── Networking & Web ──────────────────────────────────────────
requests>=2.28.0
pywebview>=5.0          # Native desktop window for jarvis_desktop.py
beautifulsoup4>=4.11.0
lxml>=4.9.0
html5lib>=1.1

# ── Information retrieval ─────────────────────────────────────
wikipedia>=1.4.0

# ── Voice / STT ───────────────────────────────────────────────
SpeechRecognition>=3.10.0
sounddevice>=0.4.5
numpy>=1.21.0
scipy>=1.9.0

# Offline STT (download model separately from alphacephei.com/vosk)
vosk>=0.3.45

# VAD — webrtcvad needs pre-built wheels on Windows
webrtcvad>=2.0.10; platform_system != "Windows"
webrtcvad-wheels>=2.0.14; platform_system == "Windows"

# ── TTS ───────────────────────────────────────────────────────
edge-tts>=6.1.0        # Free Microsoft Edge TTS (no API key)
pygame>=2.5.0          # Audio playback for edge-tts output
gtts>=2.5.0            # Google TTS fallback (free)
pyttsx3>=2.90          # Offline TTS fallback

# ── Audio processing ──────────────────────────────────────────
librosa>=0.9.0

# ── Security / Biometrics ─────────────────────────────────────
cryptography>=41.0.0
Pillow>=9.0.0
ImageHash>=4.3.1
# Optional: face-recognition provides robust face embeddings but requires dlib
# and native build tooling on Windows. Install only if desired.
# face-recognition>=1.3.0

# ── Computer Vision (optional — used for OCR & camera commands)
opencv-python>=4.8.0
pytesseract>=0.3.10

# ── Calendar integration (optional — requires Google credentials)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# ── Android (handled automatically by buildozer — do not install manually)
# android
# pyjnius
# python-for-android

# ── Azure STT (optional — only needed if you have AZURE_SPEECH_KEY set)
# azure-cognitiveservices-speech>=1.30.0

# ── ML (optional — only needed for neural retraining features)
# torch>=1.12.0
# transformers>=4.30.0
# tensorflow>=2.13.0
