# JARVIS AI Assistant — Unified Requirements
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────
python-dotenv==1.0.1
requests==2.31.0
bcrypt==4.2.0

# ── Web UI ────────────────────────────────────────────────────
flask==3.0.3
flask-limiter==3.8.0
SQLAlchemy==2.0.51

# ── STT ───────────────────────────────────────────────────────
SpeechRecognition==3.10.4
sounddevice==0.4.7
numpy==1.26.4

# Offline STT (download model from alphacephei.com/vosk)
vosk==0.3.45

# VAD
webrtcvad==2.0.10; platform_system != "Windows"
webrtcvad-wheels==2.0.14; platform_system == "Windows"

# Azure STT (optional — only if AZURE_SPEECH_KEY is set)
# azure-cognitiveservices-speech==1.38.0

# ── TTS ───────────────────────────────────────────────────────
pyttsx3==2.90
edge-tts==6.1.12
gtts==2.5.3
pygame==2.6.1

# ── AI LLM clients ────────────────────────────────────────────
groq==0.9.0
google-generativeai==0.7.2

# ── Voice ID (speaker recognition) ──────────────────────────
librosa==0.10.2
scikit-learn==1.5.2

# ── Testing ───────────────────────────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0

# ── Document Intelligence ──────────────────────────────────────
# PDF processing
PyPDF2==3.0.1

# OCR and image processing
pytesseract==0.3.10
Pillow==10.4.0

# ── Vision System ────────────────────────────────────────────────────────
# Camera and computer vision
opencv-python==4.10.0.84

# QR code scanning
pyzbar==0.1.9

# Alternative OCR engine (optional)
# rapidocr-onnxruntime==1.3.22
